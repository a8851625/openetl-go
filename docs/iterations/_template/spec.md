# IT-N：<迭代主题>

> 状态：`queued` | 依赖：<前置迭代或"无"> | 归属 roadmap 条目：<ID 列表>

## 问题陈述

<为什么这个迭代存在。描述当前的事实缺口与它造成的后果，不描述解决方案。
每条断言必须可追溯到代码位置、审计结论或运行记录。>

## 可观察结果

<迭代完成后，用户/运维视角能观察到的变化。用"能/不能"表述，不用"实现了"。>

1.
2.

## 验收标准

<可判定的编号项。每项必须能回答"怎么知道它通过了"。>

| # | 验收标准 | 判定方式 |
| --- | --- | --- |
| 1 | | |

## 非目标

<明确排除的内容，防止范围蔓延。>

-

## 交付约束

<本迭代特有的、不可违反的规则。全局约束见 ../README.md#全局交付约束，此处只写增量。>

-

## 依赖与前置

| 类型 | 内容 | 状态 |
| --- | --- | --- |
| 迭代依赖 | | |
| 外部输入 | | |
| 待决策 | | |

## 完成定义（DoD）

迭代置 `delivered` 需同时满足：

1. `tasks.md` 中全部 task 状态为 `done`，且每项有实际执行的证据记录。
2. 上方验收标准全部 `passed`；`skipped` / `blocked` 项必须转为有界后续并在 ROADMAP 留痕。
3. 归属的 ROADMAP 条目验收矩阵已回填，状态已更新。
4. 相关组件文档、runbook、maturity 元数据在同一交付内更新。
5. `git diff --check` 通过；无 secret、生成产物或调试开关进入错误位面。
